import styled from "styled-components";

const Wrapper = styled.main`
  footer {
    position: absolute;
    text-align: center;
    bottom: 0;
    width: 100%;
    height: 2.5rem;
  }
  footer p {
    color: #ccc;
  }
`;

export default Wrapper;
